import { r as registerInstance, h } from './index-5e2fb685.js';

const productCardCss = ":host{display:block}.card{display:block;box-shadow:0 4px 8px 0 rgba(0,0,0,0.2);transition:0.3s;border-radius:5px}img{width:100%;border-radius:5px 5px 0 0}.card:hover{box-shadow:0 8px 16px 0 rgba(0,0,0,0.4)}.container{padding:16px 24px}";

const ProductCard = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.isFavourite = false;
    }
    log(event) {
        console.dir(event);
    }
    isChecked() {
        return this.isFavourite;
    }
    render() {
        return (h("div", { class: "card" }, h("img", { src: this.img, alt: this.name }), h("div", { class: "container" }, h("h4", { class: "product-name" }, h("strong", null, this.name)), h("p", { class: "product-description" }, this.description), h("p", { class: "product-price" }, this.price), h("slot", null))));
    }
};
ProductCard.style = productCardCss;

export { ProductCard as product_card };
